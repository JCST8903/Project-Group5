<?php
require 'config.php';

$nombre = $_POST['nombre'];
$fono = $_POST['fono'];
$email = $_POST['email'];

$sql = "INSERT INTO contactos (nombre, fono, email) VALUES ('$nombre', '$fono', '$email')";

if ($conn->query($sql) === TRUE) {
    echo "✅ Contacto agregado correctamente.";
} else {
    echo "❌ Error: " . $conn->error;
}

$conn->close();
?>
